# offer

This project is structured as follows:

```
offer/
├── supabase/
│   ├── migrations/
│   │   └── 001_init.sql
│   └── config.toml   (optional)
│
├── frontend/         (React / Flutter / სხვა)
│
├── backend/          (optional თუ Edge Functions არ იყენებ)
│
└── README.md
```